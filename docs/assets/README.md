# 演示资源目录

将录屏导出为 GIF 或 PNG 后放在本目录，即可在仓库根目录 `README.md` 中通过相对路径引用，例如：

- `workspace.gif` — 工作台：选项目、切分支、浏览代码树
- `knife4j.gif` — Knife4j / OpenAPI 在线调试
- `docker-run.gif` — 拉镜像、起容器、健康检查

建议使用较短时长、适当压缩（如 `ffmpeg` / ScreenToGif），避免仓库体积过大。
